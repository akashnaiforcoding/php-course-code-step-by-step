<?php
    // $str = "Akki";
    // echo var_dump($str);

    // $num = 10;
    // echo var_dump($num);

    // $bool = true;
    // echo var_dump($bool);

    // $float = 13.232;
    // echo var_dump($float);

    // $arr = ["akki", "vivek", "prince", "ansh"];
    // echo var_dump($arr);

    // $empty = NULL;
    // echo var_dump($empty);

    // $user = new className(); // creates object of specific class which we have to create by self

    // Resource -> special type of datatype, we can store connections like database and other in this
    // $connection = ftp_connect("127.0.0.1") or die("Localhost not found");
    // echo var_dump($connection);

?>